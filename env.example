# Copy this file to .env and fill in your values

# ============================================
# Registry Mode (Automatic - Don't Set This)
# ============================================
# REGISTRY_MODE is automatically set by make commands:
#   - make local-docker sets REGISTRY_MODE=local
#   - make local-full sets REGISTRY_MODE=ecr
# 
# Both configurations are defined in conf/worker.yaml and the
# appropriate one is selected based on which command you run.

# ============================================
# For Local Docker Workflows
# ============================================
# You can run local Docker workflows without setting any of these variables!
# Just run: make local-docker

# ============================================
# For DigitalOcean Workflows (Optional)
# ============================================
# Only required if running workflows with RunnerType=DigitalOcean

# DigitalOcean API token (required for DigitalOcean workflows)
# DIGITALOCEAN_TOKEN=your_digitalocean_token_here

# Tailscale credentials (required for DigitalOcean workflows)
# TS_NODE_AUTH_KEY=your_tailscale_node_auth_key
# TS_SERVER_OAUTH_SECRET=your_tailscale_server_oauth_secret

# ============================================
# For AWS ECR Registry (Optional)
# ============================================
# Only required if using registry type "ecr" in conf/worker.yaml
# AWS credentials will be inherited from your AWS CLI session
# You don't need to set these manually if you're using AWS SSO

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
# AWS_REGION=us-east-1